# Test project shells

WP00 creates the required test-project topology without a test framework or test adapter. Adding a test dependency requires human approval and registration in `docs/implementation/dependency-register.md`.
